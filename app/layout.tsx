import type { Metadata } from 'next'
import { Inter } from 'next/font/google'
import './globals.css'

const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'Han Li - Software Engineer',
  description: 'Software Engineer specializing in iOS development, full-stack development, and building scalable applications. Currently at VOID Tech and pursuing Computer Science at University of Michigan.',
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  )
}

